$(document).ready(function() {
    'use strict';

    $('.portfolio-items').mixItUp({
        selectors: {
            filter: '.portfolio-filter',
            sort: '',
            target: '.mix'
        }
    });
});
